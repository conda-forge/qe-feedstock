c_compiler_version: 
  - 11
fortran_compiler_version:
  - 11
c_compiler:
  - if: linux
    then: gcc
  - if: osx
    then: clang
c_stdlib:
  - if: linux
    then: sysroot
  - if: osx
    then: macosx_deployment_target
fortran_compiler:
  - if: linux
    then: gfortran
  - if: osx
    then: gfortran
