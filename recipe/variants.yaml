c_compiler_version: 
  - 11
fortran_compiler_version:
  - 11
c_compiler:
  - if: linux
    then: gcc
  - if: osx
    then: clang
fortran_compiler:
  - if: linux
    then: gfortran
  - if: osx
    then: gfortran
