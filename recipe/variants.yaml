c_compiler_version: 
  - 15
fortran_compiler_version:
  - 15
c_compiler:
  - if: linux
    then: gcc
  - if: osx
    then: clang
fortran_compiler:
  - if: linux
    then: gfortran
  - if: osx
    then: gfortran
