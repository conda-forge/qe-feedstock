c_compiler_version: 
  - 11
fortran_compiler_version:
  - 11
c_compiler:
  - if: linux
    then: gcc
  - if: osx
    then: clang
c_stdlib:
  - if: linux
    then: sysroot
  - if: osx
    then: macosx_deployment_target
c_stdlib_version:
  - if: linux
    then: '2.17'
  - if: osx
    then: '10.13'
fortran_compiler:
  - if: linux
    then: gfortran
  - if: osx
    then: gfortran
